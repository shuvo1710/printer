{
  "name": "printer",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild server.js --bundle --platform=node --outfile=server-out.js",
    "build_dev": "esbuild server.js --bundle --platform=node --minify --outfile=server-out.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "escpos": "^3.0.0-alpha.6",
    "escpos-usb": "^3.0.0-alpha.4",
    "express": "^4.21.1",
    "esbuild": "0.17.17"
    },
  "overrides": {
    "usb": "^1.9.2"
  }
}
